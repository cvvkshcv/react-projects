import { useReducer } from 'react';
import './App.css';

function App() {
  return (
    <div className="shop">
      <h1>useReducer</h1>
      <h1>0</h1>
      <button onClick={() => {}}>Increment</button>
      <button onClick={() => {}}>Increment by 2</button>
      <button onClick={() => {}}>Increment by 4</button>
      <button onClick={() => {}}>Decrement</button>
    </div>
  );
}

export default App;
